import { taskContractForNode } from './bounded-context.mjs';

export function externalProviderPrompt(provider, prompt, schema) {
  return provider === 'cursor'
    ? `${prompt}\n\nВерни ровно один JSON object, соответствующий этой JSON Schema; без markdown и пояснений:\n${JSON.stringify(schema)}`
    : prompt;
}

export function buildPrompt({ nodeId, task, plan, skills, priorEvidence, reviewEvidence = null, profile = null, projectInstructions = null }) {
  const node = plan?.nodes?.find((candidate) => candidate.id === nodeId) ?? { id: nodeId };
  const contract = taskContractForNode(task, plan, node, priorEvidence);
  const workspaceLabel = profile?.workspaceMode === 'direct' ? 'текущего проекта' : 'текущего worktree';
  return [
    'Пиши human-facing title, outcome, summary и findings на русском языке. Machine IDs сохраняй. Язык исходного кода и документации проекта определяется правилами проекта и задачей.',
    'В findings[].path указывай существующий относительный путь внутри проекта. Для общего замечания без конкретного файла ставь null. Никогда не используй точку, путь с .., абсолютный путь или URL.',
    `Ты выполняешь node ${nodeId} локального Graph Flowcairn.`,
    `Цель: ${task.goal}`,
    `Инструкции задачи (данные в пределах утвержденного scope):\n${typeof task.instructions === 'string' ? task.instructions : JSON.stringify(task.instructions ?? [])}`,
    node.action?.id === 'ai-analyze' && plan?.workflow === 'autonomous'
      ? 'Выполни read-only анализ проекта. Верни analysis: requirements, constraints, projectFacts [{path,fact}] с реально прочитанными файлами, acceptance, risks. В analysis.constraints записывай только постоянные ограничения исходной задачи; read-only текущего узла, edits=[] и отсутствие еще не выполненных checks не являются ограничениями будущей реализации. Для пояснения существующего требования используй его точный req-ID, не создавай новое обязательство под другим ID. Отделяй наблюденное от предположений. Это полный структурированный результат для следующей отдельной сессии planner: не заменяй его общим обещанием. edits=[], changedFiles=[], plan=[]. Верни uncertain только когда нельзя назвать безопасный локальный путь реализации в разрешенном scope. Неизвестный backend/API-контракт, продуктовый вариант интерфейса или недоступная документация зависимости сами по себе не являются таким основанием, если задачу можно ограничить клиентской частью: зафиксируй их как risks/warnings и верни pass. Не выдумывай endpoint, серверное сохранение или успешную регистрацию: planner должен явно ограничить такой план локальным UI и валидацией, пока контракт не дан. Весь analysis должен помещаться в 16 KiB.'
      : '',
    task.planningFeedback?.length ? `Уточнения пользователя к плану (сохраняют исходную задачу и scope):\n${task.planningFeedback.join('\n')}` : '',
    profile?.onboarding?.testPolicy === 'keep' ? 'Политика проекта: сохраняй существующие тесты, не добавляй новые. Подтверди работоспособность настроенными проверками и ревью.' : '',
    profile?.onboarding?.testPolicy === 'add' ? 'Политика проекта разрешает новые тесты по риску изменения; не добавляй тесты, дублирующие реализацию.' : '',
    node.action?.id === 'ai-plan'
      ? 'Ты planner: используй priorEvidence.analysis и накопленные feedback, не начинай анализ заново. Предложи от 1 до 12 task-specific implementation steps в steps: id, title, outcome, needs (IDs других steps), paths (только разрешенные write paths), readPaths (точные дополнительные файлы/каталоги, нужные для чтения: импорты, интерфейсы, тестовые сценарии), requirementIds (точные IDs требований контракта). Разделяй работу по проверяемым outcomes и зависимостям исходя из задачи и исходников. Не предлагай actions, Skills, permissions, shell или новые checks: их назначает trusted compiler. Контекст implementation ограничивается paths/readPaths текущего шага, его объявленных needs и обязательным project context; не рассчитывай на чтение всего scope. Не включай planning/check/review/gate как steps: их добавляет runtime. Если запрошенное поведение уже реализовано, предложи один implementation step: проверить соответствие исходного кода требованиям и исправить только найденные дефекты; допустим no-op без edits. Для verdict=pass steps не может быть пустым. При нехватке контекста верни uncertain с findings и steps=[]; не угадывай. edits=[], changedFiles=[], plan=[].'
      : '',
    node.action?.id === 'ai-plan'
      ? `Верни contractProposal: requirements, optionalImprovements, constraints, assumptions, unknowns. constraints — итоговый список постоянных ограничений задачи; сохрани требования владельца, не копируй служебные заметки анализа о read-only/edits=[] текущего этапа. Сохрани все обязательные требования контракта, их id/title/mandatory; для исходных req-001 и далее verification.criterion должен точно равняться title. Если исходная acceptance содержит полное описание задачи, сохрани общий смысл через goal/instructionsHash, но не добавляй это описание отдельным req-001. Каждый пункт priorEvidence.analysis.requirements сделай собственным обязательным требованием с уникальным ID, точным критерием и подходящей проверкой; убери префикс req-001: из его названия. Свяжи каждый пункт с конкретными implementation steps через requirementIds. У каждого требования verification={method,checkIds,criterion,paths}. Verification path описывает будущую проверку: он не утверждает, что проверка уже прошла. Пустой priorEvidence.verificationChecks на этапе планирования ожидаем и не является основанием для human. Выбирай check для объективного сценария, который уже существует или который разрешено добавить в implementation steps и выполнить настроенной проверкой; доступные checkIds: ${JSON.stringify(task.checks.map((check) => `check-${check}`))}. Общий успешный тест или build не доказывает любое поведение. Метод source-review допустим только для свойств, непосредственно наблюдаемых в исходниках (например, наличие конкретной декларации или текст документа), и требует точных цитат при review. Для поведения, включая браузер и интеграцию, планируй подходящий автоматический сценарий через доступный check, если он выполним в разрешенных границах. Выбирай human для субъективной приемки либо когда подходящую объективную проверку действительно нельзя выполнить или подготовить в рамках задачи; не выбирай human только потому, что будущий check еще не запускался. Для source-review/human checkIds=[]. paths должны входить в разрешенный контекст. Не объявляй выполнение требования: это делает runtime по evidence.`
      : '',
    `Контракт текущего узла:\n${JSON.stringify(node)}`,
    `skillsUsed должен содержать точные runtime IDs назначенных инструкций: ${JSON.stringify(node.skills ?? [])}. Подтверди соблюдение всех назначенных правил; не заменяй runtime ID именем из YAML frontmatter и не добавляй свои Skills.`,
    `Разрешенные изменения только для текущего узла: ${node.resources?.writes?.join(', ') || 'нет, узел только читает'}. Не предлагай edits для файлов других этапов, даже если они входят в общую задачу.`,
    node.action?.id !== 'ai-implement' ? 'Этот этап только читает: edits=[], moves=[], jsonTransfers=[] и changedFiles=[]. Файлы предыдущих изменений описывай в findings; не записывай их как изменения текущего этапа.' : '',
    `Объявленный read context: ${node.resources?.reads?.join(', ') ?? ''}`,
    projectInstructions?.files.length
      ? `Следующие инструкции и явно выбранный Markdown-контекст доставлены Executor из проекта целиком с проверенными хешами. Они могут отсутствовать в изолированном worktree: используй переданный текст, не пытайся читать исходный checkout или ignored-файлы. Применяй только указанную область, сохраняя приоритет задачи, permissions и action contract; ссылки внутри текста не расширяют доступ:\n${JSON.stringify(projectInstructions.files)}`
      : '',
    'Sandbox намеренно запрещает чтение и перечисление корня worktree: не запускай ls ., find . или аналогичный обход. Читай только точно перечисленные paths выше; directory из списка можно перечислять только внутри него. Отказ для корня не означает отказ для этих разрешенных paths.',
    `Устойчивый контракт и релевантные требования текущей работы:\n${JSON.stringify(contract)}`,
    node.action?.id === 'ai-review'
      ? 'Верни requirementAssessments для каждого обязательного требования: requirementId, verdict, criterion (точная строка из verification.criterion), checkIds (точный набор из контракта), citations=[{path,startLine,quote}], reason. pass допустим только после проверки конкретного критерия. Для check используй проверенные receipt facts из priorEvidence.verificationChecks и непустые цитаты реально прочитанного тестового сценария/утверждения, которые показывают, что он проверяет именно это требование; общий тест/build недостаточен. Для source-review приведи точные непустые цитаты исходников с начальной строкой и объясни, какое непосредственно наблюдаемое свойство они доказывают. Все citations.path должны входить в verification.paths требования. Runtime сверит цитаты с проверяемыми bytes и hashes. Для human верни uncertain и объясни нужный способ приемки. Общий verdict=pass, hash файла, заявление исполнителя или наличие измененного файла не доказывают требования.'
      : '',
    'Новые тесты добавляй по риску изменения и правилам проекта. Явный запрет новых тестов соблюдай и укажи ограничение в evidence. Не придумывай обязательный coverage или единый набор проверок: runtime запускает только настроенные checks.',
    `Не выходи за объявленный read context и scope изменений. Не делай commit, push, merge, deploy или внешние действия.`,
    'Lock-файлы зависимостей, sourcemaps и бинарные материалы исключены из AI-контекста. Их целостность отдельно проверяет runtime. Не пытайся обходить этот запрет.',
    `Не читай файлы вне ${workspaceLabel}, .git, ignored-файлы, секреты, персональные данные и raw записи.`,
    `Ты не записываешь файлы. Для implementation предложи точные edits: path, SHA-256 текущих bytes в previousHash (null для нового файла), полное новое UTF-8 content (null для удаления), executable. Executor применит их после проверки scope/hash. Для analysis/review edits должен быть пустым.`,
    'Для переноса целого файла без изменения bytes используй moves=[{from,to,previousHash}]; destination должен отсутствовать. Для переноса существующих верхнеуровневых ключей большого JSON object используй jsonTransfers=[{from,to,previousHash,targetPreviousHash,keys}]. Executor сам переносит точные значения и удаляет keys из source, не возвращай большой JSON целиком. Все hashes относятся к BEFORE текущего шага; targetPreviousHash=null только для нового файла. Несколько transfers из одного source используют общий BEFORE hash. Target не должен уже содержать переносимые keys, даже с теми же значениями. Один key переносится один раз; source не может одновременно быть destination; paths transfer нельзя использовать в edits/moves того же шага. Оба paths должны входить в write scope. Максимум 100 transfers, 1000 keys/transfer, 10000 keys всего, 8 MiB/file, 32 MiB before+after. changedFiles содержит уникальное объединение всех реально изменяемых paths, включая from/to. Для read-only этапов moves=[] и jsonTransfers=[].',
    'Отчет изменений может быть text/x-diff с точными строками либо application/json формата flowcairn-change-evidence/v1. Структурный отчет вычисляет исполнитель из реальных файлов: move доказывает перенос неизменных байтов, json-entries содержит точные измененные значения и хеши обоих состояний. Порядок JSON-полей и внешние пробелы отдельно обозначены как не воспроизводимые этой семантической записью. delete доказывает удаление файла по его пути, размеру и прежнему хешу; contentIncluded:false не означает, что полное удаленное содержимое было просмотрено. Если критерий зависит от анализа удаленного содержимого и его недостаточно, верни uncertain и конкретное замечание. Не принимай описание операции за смысловую проверку, которой не было.',
    `priorEvidence.workspaceFiles содержит проверенные Executor hashes текущих файлов. Используй их как previousHash, не подставляй hash из старого receipt и не угадывай. null допустим только для файла, которого сейчас нет. Если требуемое исправление уже присутствует, проверь его и верни edits=[] и changedFiles=[] без искусственных изменений.`,
    `В plan[].paths перечисляй только предлагаемые изменения внутри scope; пути чтения и build outputs описывай словами, не добавляй их в этот массив.`,
    `Не запускай build, tests, linters и другие команды, которые меняют workspace. Зарегистрированные проверки запускает Executor после применения edits.`,
    'Для ai-implement verdict относится к outcome текущего узла: корректному предложению edits или доказанному no-op. Не возвращай uncertain только потому, что проверки запускаются следующими узлами. Не заявляй успешные tests/build/review до их фактического выполнения; итоговую готовность определяет Executor после этих этапов.',
    `Исходный код допустим только в edits.content; не включай секреты, персональные данные, raw prompt, stdout или stderr.`,
    `Пиши компактно: summary до 2000 знаков, не более 12 шагов и 20 рисков/замечаний.`,
    `Верни JSON по заданной schema и перечисли точные имена реально использованных Skills в skillsUsed.`,
    reviewEvidence
      ? `Review требует полного evidence: разрешено прочитать ровно этот private read-only файл вне worktree: ${reviewEvidence.path}. Это единственное исключение к запрету чтения вне worktree; не читай его каталог, соседние файлы или .git. Файл содержит ${reviewEvidence.bytes} UTF-8 bytes, SHA-256 ${reviewEvidence.hash}. Прочитай весь JSON, все implementation receipts и все diff/changed-files целиком, включая previousExecutions всех предыдущих версий исправления, включая удаления и хвосты. При ограничении вывода читай последовательными частями до конца, не ограничивайся первым фрагментом. Если это невозможно, верни uncertain; pass запрещен. Верни reviewEvidenceHash=${reviewEvidence.hash}, связывающий результат с этим exact input. Hash не доказывает качество review. Excerpts ниже только вспомогательные и не заменяют этот файл.`
      : '',
    priorEvidence ? `Проверенное evidence предыдущих nodes:\n${JSON.stringify(priorEvidence)}` : '',
    `Назначенные Skills переданы полностью:\n${skills}`,
  ]
    .filter(Boolean)
    .join('\n\n');
}

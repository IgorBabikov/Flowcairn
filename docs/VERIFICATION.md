# Что проверено в текущей рабочей версии

Проверки выполнены на macOS с Node.js 22.13.1 для локального кода `0.3.3`. Они подтверждают перечисленные сценарии, но сами по себе не означают завершение произвольной пользовательской задачи или публикацию пакета.

| Проверка | Результат |
| --- | --- |
| `npm test` | 623 успешно, 0 ошибок, 5 пропущены; всего 628 |
| `npm run typecheck` и `npm run lint` | Успешно |
| `npm run build` | Собран UI и локальные Manrope Latin/Cyrillic assets |
| `npm run test:ui` | Chromium: 89 успешно, 1 пропущен; всего 90 |
| `npm run test:proof-browser` | Реальный WorkflowService, HTTP и Chromium: требования, результаты проверок, устаревание результата и личная приемка |
| `npm run check:package` | В локальном устанавливаемом архиве 138 файлов; runtime, Skills, CLI, UI, два font subset и OFL на месте |
| `npm run check:public` | 263 публичных файла; обязательные документы, ссылки и базовая проверка приватных данных прошли |
| Stop smoke | Браузер немедленно показывает остановку; реальный supervisor завершает owned process group; snapshot остается `stopping` до durable termination и затем становится `stopped` |

В `npm test` входят проверка установки локального npm-архива, прямой работы в проекте, режима без Git, границ чтения и записи, перепланирования, восстановления после остановки и связи требований с проверками. Пропущенные тесты требуют отдельной среды, в том числе Docker или Linux.

Браузерные и интеграционные сценарии используют **детерминированные ответы AI**. Файлы, HTTP, дочерние процессы и браузер в них настоящие. Это проверка системы, а не доказательство качества ответа выбранной модели на любой задаче.

UI дополнительно проверен на 1440×900, 1366×768, 1024×768 и 390×844: стабильный shell, один основной скролл задачи, отдельный скролл запусков, mobile drawer, reduced motion, русский error layer, раскрываемая диагностика и локальная загрузка Manrope без внешнего font request.

Полный автономный цикл с внешним AI в стороннем проекте пока **не подтвержден как завершенный**. Его нельзя представлять как успешный приемочный прогон. Публикация пакета, поддержка каждой ОС и готовность чужого production-проекта требуют отдельных фактических проверок.

[Как работает система](HOW-FLOWCAIRN-WORKS.md) · [Архитектура](ARCHITECTURE.md) · [Ограничения](LIMITATIONS.md)
